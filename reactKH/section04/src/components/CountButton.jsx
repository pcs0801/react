

function CountButton({ CountButton }) {
    return (
        <div>
            <button style={{ border: "1px solid black" }} onClick={CountButton}>
                +
            </button>
        </div >
    );
};

export default CountButton;